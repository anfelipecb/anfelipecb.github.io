# this file uses prebuilt image in dockerhub
services:
  jekyll:
    build: .
    # image: amirpourmand/al-folio  # Commented out to force build
    ports:
      # Host 4000 → container 8080 (change first number if 4000 is busy)
      - "4000:8080"
    volumes:
      - .:/srv/jekyll
